---
import { loadMonthlyReports } from "../../lib/monthly";

const latest = loadMonthlyReports()[0];
if (!latest) throw new Error("No monthly report is available for the latest entrypoint.");
return Astro.redirect(`/monthly/${latest.slug}/`, 302);
---
