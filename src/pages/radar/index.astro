---
import { loadRadarReports } from "../../lib/radar";

const latest = loadRadarReports()[0];
if (!latest) throw new Error("No radar report is available for the latest entrypoint.");
return Astro.redirect(`/radar/${latest.slug}/`, 302);
---
