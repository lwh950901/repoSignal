---
import { loadDailyReports } from "../../lib/digests";

const latest = loadDailyReports()[0];
if (!latest) throw new Error("No daily report is available for the latest entrypoint.");
return Astro.redirect(`/daily/${latest.slug}/`, 302);
---
