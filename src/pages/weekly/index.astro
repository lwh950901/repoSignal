---
import { loadWeeklyReports } from "../../lib/digests";

const latest = loadWeeklyReports()[0];
if (!latest) throw new Error("No weekly report is available for the latest entrypoint.");
return Astro.redirect(`/weekly/${latest.slug}/`, 302);
---
