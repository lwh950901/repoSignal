# GitHub 项目组合可行性方案｜2026-09-15

> 由定时任务自动生成（`scripts/opportunity_analysis.py`）：以今日日报项目为锚点、结合最近 90 天项目池拼出的多项目组合可行性研究草稿；不包含代码，不是公开结论，落地前需逐个组件复核。
> 输入：今日锚点 `daily/2026-09-15.md`（3 个项目）；组合池：最近 90 天（2026-06-17 ~ 2026-09-15）340 个唯一项目（日报 68 份 / 周报 11 份）。方案 1-3 个：最多 1 个成熟方向（固定模板命中）+ 最多 2 个探索方向（今日锚点拼接，待验证）；没有合格探索方向时少于 3 个。
> 评分（0-100，确定性规则，用于方案横向比较，不代表商业结论）：组件可靠度 35 · 组件供给 15 · 风险敞口 15 · 今日锚点 15 · 来源多样性 10 · 许可证 5 · 完整度 5；档位：≥85 高，70-84 中，<70 低。
> 验证路径（固定）：每个组件按来源报告的'上手建议/真实风险'复核（固定版本、隔离环境、自有数据复测）。
> 新鲜度规则：以下方案已连续出现两天，本轮跳过（同一方案最多连续两天）：企业私有化知识助手。
> 探索方向：由今日锚点直接拼接，组件全部来自今日日报；已标注为待验证，只说明组件可拼装，不代表市场需求成立。

## 可行性方案

### 1. 可审计的 Agent 开发/交付平台（CI 化 Agent 流水线）（组合 5 个项目，今日锚点 1 个）

**方案评分**：**79/100（中）**（组件可靠度 30/35 · 组件供给 15/15 · 风险敞口 9/15 · 今日锚点 5/15 · 来源多样性 10/10 · 许可证 5/5 · 完整度 5/5）

**方案身份**：`plan_family=auditable-agent-delivery` · `variant=b0e7ba4d9489`

**业务轨道**：成熟方向（组件供给已核对）

**业务语义**：`track=mature` · `customer=已在使用 coding agent 的研发团队与平台工程组` · `problem=Agent 跑挂了无法恢复，花了多少钱、有没有越权也说不清` · `workflow=沙箱内执行 → 留痕与审计报告 → 人工批准后写仓库` · `delivery=团队内自建流水线，接入现有 CI` · `expected_outcome=像 CI 一样可恢复、可审计的 Agent 交付流水线` · `evidence_status=supply-checked`

**业务定位**：让 Agent 任务像 CI 一样跑：进沙箱执行、全程留痕、出审计报告，人工批准后才允许写仓库。

**目标客户**：已在使用 coding agent（Codex/Claude Code/Cursor）的研发团队与平台工程组。

**市场机会**：Agent 开始真正干活后，'跑挂了怎么恢复、花了多少钱、有没有越权'成为刚需；近 90 天池里隔离执行和观测组件明显变多。

**可行性依据**：长任务 Agent 的失败恢复、可观测、安全执行是池里反复出现的主题，组件已成熟，适合拼成 CI 流水线。 本组合含今日发现项目 1 个（`earthtojake/text-to-cad`）；池中各能力面候选充足（sandbox 149 · observability 130 · security 106 · agent 301 · gateway 174），最稀缺槽位也有 106 个候选。

**组合方案**：

| 角色 | 项目 | 入选理由 |
|---|---|---|
| 执行沙箱/隔离 | [`vstorm-co/pydantic-ai-backend`](https://github.com/vstorm-co/pydantic-ai-backend) | 虽然 Stars 不高，但它解决的是 Agent 落地的关键薄弱环节，维护状态和质量证据强，符合“潜力型”而非按 Stars 排序的推荐原则。 |
| 可观测与评测 | [`comet-ml/opik`](https://github.com/comet-ml/opik) | Apache-2.0、自托管、安装入口、测试与持续 Release 同时成立，能直接补齐 Agent 项目最容易缺失的可观测和评测闭环。 |
| 安全审计 | [`tencent/ai-infra-guard`](https://github.com/Tencent/AI-Infra-Guard) | 它的差异化不是“又一个 Agent 框架”，而是把 Agent 生态安全拆成可组合扫描面；对想做内部 AI 安全基线、MCP/Skill… |
| Agent 编排 | [`earthtojake/text-to-cad`](https://github.com/earthtojake/text-to-cad) | 它把泛化 Agent 能力落到可验证的工程工具链，适合作为垂直行业工作流和工具调用协议的二次开发底座。 |
| 模型网关/成本 | [`moltis-org/moltis`](https://github.com/moltis-org/moltis) | 相较同类高星 Agent/Memory 仓库，它的 Rust 单二进制、安全架构、Release 验证和多渠道边界更有学习价值… |

**差异化**：相比单个 harness 或观测工具，它把隔离执行、trace 评测和审计报告串成一条流水线。

**MVP 范围（做什么，不含代码）**：受限流水线：沙箱内跑一次任务、记录 trace、输出审计报告，人工审批后才允许写仓库；成本与多 Agent 编排后置。

**主要风险（来源报告）**：

- `vstorm-co/pydantic-ai-backend`（执行沙箱/隔离）：社区规模小，仍需自行评估 sandbox 逃逸、路径规则覆盖和多租户威胁模型；强绑定 Pydantic AI 生态。
- `comet-ml/opik`（可观测与评测）：可观测平台会接触 Prompt、输入输出和用户数据；自托管仍需审计存储、权限、脱敏和数据保留策略；LLM-as-a-judge 结果不能替代领域人工验收。
- `tencent/ai-infra-guard`（安全审计）：当前 Stars 低于爆发型 5,000 下限，因此只归入潜力型；安全扫描结果不是完备证明，规则库覆盖、误报/漏报、漏洞库更新和部署数据权限仍需自行验证。
- `earthtojake/text-to-cad`（Agent 编排）：仍处于 v0.x，依赖的 CAD 运行环境和大文件资产会提高复现成本；设计结果必须经过专业工程校核。
- `moltis-org/moltis`（模型网关/成本）：仍是年轻项目，Issue/PR 比例不低；集成面很宽，实际安全模型、沙箱隔离和多渠道凭据管理需要本地审计后再用于敏感工作流。

### 2. 文档智能 · 可观测 · Agent 工作台（组合 3 个项目，今日锚点 3 个）

**方案评分**：**89/100（高）**（组件可靠度 31/35 · 组件供给 15/15 · 风险敞口 15/15 · 今日锚点 15/15 · 来源多样性 3/10 · 许可证 5/5 · 完整度 5/5）

**方案身份**：`plan_family=biz-01682fb7aaf2` · `variant=40e340c77f14`

**业务轨道**：探索方向（待验证）

**业务语义**：`track=exploratory` · `customer=文档密集的业务团队` · `problem=跑起来的质量与花费无法评测` · `workflow=记录一次真实任务 → 建评测集 → 回归对比` · `delivery=按管线交付，不改变现有文档入口` · `expected_outcome=质量与花费可评测、可回归` · `evidence_status=to-validate`

**待验证说明**：组件可拼装 ≠ 需求成立；本方向需先小范围试用并记录产出，再判断是否值得投入。

**业务定位**：按'文档智能 · 可观测 · Agent 工作台'方向，把今日新发现的 3 个项目先拼成可试用组合：为实时分析、金融行情和遥测数据设计的高性能开源时序数据库。（`questdb/questdb`）、覆盖文字检测、识别、版面解析与文档智能的 OCR 工具链。（`paddlepaddle/paddleocr`）、把自然语言需求转成 CAD、CAE 与 CAM 操作的 Agent Skills 工具集。（`earthtojake/text-to-cad`）。方向与需求都待验证，先各自试用、记录产出，再判断能否打通。

**目标客户**：文档密集的业务团队

**市场机会**：（待验证假设）客户问题：跑起来的质量与花费无法评测；预期结果：质量与花费可评测、可回归。今日 3 个锚点分属 观测、文档、Agent 等能力面，池中对应候选 130 个——这只说明组件可拼装，不代表市场需求成立；先小范围试用再判断。

**可行性依据**：全部组件来自今日日报，能力面尽量互补（3 个）；该方向未经过市场验证，先按业务语义里的客户问题做小范围试用。 本组合含今日发现项目 3 个（`questdb/questdb`、`paddlepaddle/paddleocr`、`earthtojake/text-to-cad`）；池中各能力面候选充足（observability 130 · document 150 · agent 301），最稀缺槽位也有 130 个候选。

**组合方案**：

| 角色 | 项目 | 入选理由 |
|---|---|---|
| 为实时分析、金融行情和遥测数据设计的高性能开源时序数据库。 | [`questdb/questdb`](https://github.com/questdb/questdb) | 它同时具备立即可运行的存储引擎形态、持续发布和面向故障路径的测试投入，适合作为实时数据产品的基础组件。 |
| 覆盖文字检测、识别、版面解析与文档智能的 OCR 工具链。 | [`paddlepaddle/paddleocr`](https://github.com/PaddlePaddle/PaddleOCR) | 源码中同时有模型接口、文档结构化、部署和测试边界，是学习“研究模型如何工程化交付”的高密度案例。 |
| 把自然语言需求转成 CAD、CAE 与 CAM 操作的 Agent Skills 工具集。 | [`earthtojake/text-to-cad`](https://github.com/earthtojake/text-to-cad) | 它把泛化 Agent 能力落到可验证的工程工具链，适合作为垂直行业工作流和工具调用协议的二次开发底座。 |

**差异化**：完全由今日新发现驱动，组件全部来自今日日报；与固定模板方向不同，属于待验证的探索方向。

**MVP 范围（做什么，不含代码）**：先分别试用各组件并记录可用产出，再打通 `questdb/questdb`、`paddlepaddle/paddleocr`、`earthtojake/text-to-cad` 之间的最小数据流或协作流；其余按试用反馈取舍。

**主要风险（来源报告）**：

- `questdb/questdb`（为实时分析、金融行情和遥测数据设计的高性能开源时序数据库。）：950 个开放 Issue 表明维护队列较大；高性能参数和数据模型需要按真实写入/查询负载压测，不能把发布基准直接当作自身结果。
- `paddlepaddle/paddleocr`（覆盖文字检测、识别、版面解析与文档智能的 OCR 工具链。）：模型、硬件后端和多模块依赖会让最小复现变重；不能把 benchmark 或特定硬件配置直接照搬到生产吞吐目标。
- `earthtojake/text-to-cad`（把自然语言需求转成 CAD、CAE 与 CAM 操作的 Agent Skills 工具集。）：仍处于 v0.x，依赖的 CAD 运行环境和大文件资产会提高复现成本；设计结果必须经过专业工程校核。

## 单点项目机会（供参考）

- `obra/superpowers`（`#agent`）→ 可包装成垂直场景的 Agent 托管/订阅产品。依据：按反馈规则允许优质 Agent 内容型项目进入，但只占一个主位；它的维护、测试和方法论完整度都明显超过普通 awesome list。
- `affaan-m/ecc`（`#agent`）→ 可拆出可复用的 Agent 编排能力做独立服务。依据：在社区信号、近期维护和可复用工程材料上都很强，最适合作为今天的爆发型项目，但评分已因复杂度和热度噪声做折扣。
- `nousresearch/hermes-agent`（`#gateway`）→ 可做模型网关/统一接入层。依据：它把个人 Agent 的“长期成长”从口号推进到网关、工具、语音、研究和版本化 Release 的组合，潜力很高；但今天更适合做受控实验与架构跟踪，而非无审计上线。
- `mattpocock/skills`（`#document`）→ 可做文档解析/转换服务。依据：作为内容型额外发现，它把工程原则转成可安装、可组合的 Agent 行为资产，且今日活动和用户反馈偏好都支持保留这一类线索。
- `microsoft/markitdown`（`#document`）→ 适合作为 RAG 上游的入库清洗组件。依据：它提供了具体、可复现的源码学习对象：转换器接口、插件边界、可选依赖、测试与安全约束；同时明确指出哪些方便的做法不宜照搬。
- `f/prompts.chat`（`#agent`）→ 可做成面向特定岗位的 Agent 工具集。依据：它兼具内容资源和真实可运行软件形态，且授权边界比普通 prompt 清单更清晰；今天不占主推荐位置，但值得作为 Agent 知识资产管理的参考实现。
- `snailclimb/javaguide`（`#document`）→ 可做成格式转换 API 或批处理工具。依据：它补足今日 Agent 控制面之外的后端基础与系统设计学习路径，且内容来源、授权和维护信号清晰，作为额外发现不挤占软件主推荐位置。
- `langflow-ai/langflow`（`#agent`）→ 可包装成垂直场景的 Agent 托管/订阅产品。依据：成熟度、安装路径、持续 Release 和文档/回归结构都足够强，能直接把 Agent/RAG 想法做成可运行流程，是今天最稳妥的实际入口之一。

## 行动建议

1. 优先推进评分最高的组合：「文档智能 · 可观测 · Agent 工作台」（89 / 100，3 组件，今日锚点 3 个）。
2. 每个组合先核验 2-3 个核心组件：许可证、维护状态、来源报告中的真实风险。
3. 投入开发前，先用目标客户访谈或小范围试用验证需求假设，再决定组合取舍。
4. 本方案由定时任务自动生成并保留历史；每周新增周报与日报后重跑，信号会自动更新。

