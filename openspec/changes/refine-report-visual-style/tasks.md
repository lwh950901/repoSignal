## 1. 确认基线与准备预览

- [x] 1.1 核对当前工作区、五类报告组件与共享样式，记录已有未提交改动；确认本次只修改界面、必要测试和本变更文档，不覆盖其他工作。
- [x] 1.2 运行 `npm test -- src/components/layout-contract.test.ts` 记录基线；定位此前预览启动失败的原因，建立可访问的本地预览。长时间运行的命令使用后台进程与日志文件，并按项目约定汇报进度。
- [x] 1.3 为日报、周报、月报、每日可行性方案和开源雷达周刊各选一个实际报告，另选一篇历史报告；记录 URL 和改动前的代表性截图，确认有评分、无评分、长仓库名、长正文和表格的覆盖情况。

## 2. 统一基础字号与颜色语义

- [x] 2.1 在 `src/styles/global.css` 中建立辅助文字、控件文字、正文与阅读宽度令牌；保留 15px 根字号、现有字体回退链、核心色板以及严格递增的中高层级间距。
- [x] 2.2 将五类报告与共享导航、归档、搜索界面的文字应用到对应角色：辅助信息至少 12px，中文控件约 13px，叙述性正文与事实描述 15–16px、行高 1.75–1.85；中文使用正文无衬线字体，仓库名、日期、评分和技术标识使用等宽字体。
- [x] 2.3 收敛分类标签与边线用色，去除普通标签旋转、黄底和强对比大色块；黄色保留给当前选择和已有明确核心标记，简介及展开分析改用普通边界色，并统一清晰的键盘焦点轮廓。

## 3. 优化项目层级与阅读宽度

- [x] 3.1 调整共享项目条目的内部间距与约 30px 上下内边距，保留项目分隔线并移除事实字段逐行上边框；用语义 class 将推荐内容设为 500 字重，确保字段缺失时强调不会错位。
- [x] 3.2 保留定位、简介、事实字段、完整分析的现有顺序，以及简介的独立轻量样式；确认月报事实矩阵、表格分隔、章节结构和详情默认展开状态不受影响。
- [x] 3.3 将五类报告的长段落、列表、定位、简介和长事实描述限制到默认 44em 且不超过容器宽度；保持表格、评分面板、雷达正文外层与封面的独立布局宽度，验证归档收起后的行长仍受控。
- [x] 3.4 调整长标题列、长链接与评分区的换行和最小宽度；在窄屏保留目标字号、表格局部滚动或既有移动字段布局，必要时让两行页头更早生效，避免五项导航与品牌、搜索相互挤压。

## 4. 强化归档当前项

- [x] 4.1 基于共享时间线已有 `aria-current="page"` 绘制当前项的整行浅底、短竖条与字重；保留日期节点和时间线，确保“最新”标签不会随当前选择移动。
- [x] 4.2 为普通、当前、hover 和 focus-visible 状态预留一致空间，确认切换不造成文字跳动，焦点轮廓不被轨道滚动容器裁切。
- [x] 4.3 检查并适配桌面归档折叠按钮与移动端原生选择器，保留现有展开、日期切换和无 JavaScript 备用链接行为。

## 5. 实现真实评分的静态刻度

- [x] 5.1 核对 `ProjectRecord.score` 的类型和解析器契约；在 `ProjectEntry.astro` 中区分评分缺失、零分和有效范围，保持原始数字及满分语境，不修改数据来源或评分算法。
- [x] 5.2 为 0–100 的有限评分生成十段静态 SVG，按每段 10 分计算精确填充比例；86 分对应八段满和第九段 60%，0 分全空，100 分全满，缺失或无效范围输入不生成刻度。
- [x] 5.3 将刻度置于数字下方，使用外部 CSS 定义单色外观，设置 `aria-hidden="true"` 和 `focusable="false"`；保留“周选”语义，确认没有行内样式、客户端依赖、重复播报、额外 Tab 停靠点或 CSP 放宽。

## 6. 定向测试与静态检查

- [x] 6.1 为评分实际渲染输出增加必要测试，覆盖缺失、0、86、100、超范围与非有限值，核对数字、填充比例和无障碍属性；增加部分项目字段缺失时语义样式不发生错位的检查。
- [x] 6.2 更新 `src/components/layout-contract.test.ts` 中被本设计取代的正文宽度等具体样式断言；保留并运行导航顺序、归档复用、搜索可识别性、原生详情和静态脚本安全策略等既有契约检查，不用源码字符串断言替代浏览器视觉验收。
- [x] 6.3 运行定向布局测试、新增渲染测试、`npm run check` 和 `npm run build`，记录结果并修复与本次变更相关的失败；构建产物能够用于后续浏览器验收。

## 7. 浏览器验收与交付

- [x] 7.1 在 1440px、1024px、768px、390px、320px 下逐一查看五类实际报告和共享搜索界面，核对文字尺度、内容层级、标签、段落宽度、表格和封面布局，确认页面无整体横向溢出；记录代表性截图与检查结果。
- [x] 7.2 在桌面核对归档展开与收起、历史当前项与“最新”标签、hover、键盘焦点及详情操作；在移动端核对日期选择与导航横向滚动，确认搜索入口、结果选择和空状态仍可用。
- [x] 7.3 检查长仓库名、长链接、长段落、有无评分和部分字段缺失的布局；实际内容不足以覆盖的边界使用临时验证样例，不修改冻结报告或发布测试路由。验证外部字体不可用时的回退布局。
- [ ] 7.4 检查 200% 缩放下的键盘操作、减少动态效果和禁用 JavaScript 的阅读路径；在实际施加现有生产 CSP 的页面中确认评分刻度仍显示，静态正文、归档备用链接与原生详情可访问。
- [x] 7.5 复核差异仅覆盖已定义范围，记录测试结果、截图位置及已解决的问题；运行 `openspec validate refine-report-visual-style --strict`，仅在对应验收证据齐全后勾选任务并总结交付状态。

验收证据见 `verification.md`。7.4 的生产 CSP、无脚本路径与 200% CSS 缩放已验证；浏览器原生缩放和系统减少动态效果切换保留人工验收，未以替代检查勾选完成。
