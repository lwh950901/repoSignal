# 样式优化验收记录

验证日期：2026-09-20。本次实现保留灰绿档案布局，统一文字角色、项目间距和归档当前态，限制长文行长，并加入真实分数驱动的静态 SVG 刻度。

## 自动检查

- `npm test`：15 个测试文件、145 项测试全部通过，包括 Astro 组件实际渲染的 10 项评分和字段语义检查。
- `npm run check`：41 个文件，0 errors、0 warnings、0 hints。
- `npm run build`：131 个页面构建成功。
- `openspec validate refine-report-visual-style --strict`：通过。
- `git diff --check`：通过。

测试、检查和构建输出保存于 `evidence/reposignal-style-*.log`。

## 浏览器覆盖

实际报告：

| 类型 | 路径 |
| --- | --- |
| 日报 | `/daily/2026-09-19/` |
| 周报 | `/weekly/2026-W37/` |
| 月报 | `/monthly/2026-08/` |
| 可行性方案 | `/feasibility/2026-09-19/` |
| 开源雷达 | `/radar/2026-W37/` |
| 历史报告 | `/daily/2026-09-18/` |

五类报告分别在 1440、1024、768、390、320px 检查，共 25 个布局组合；共享搜索在同样五档宽度检查。检查点包括可见文本尺度、报告层级、换行、段落行长及整页横向溢出。结果见 `evidence/matrix.json` 和 `evidence/search.json`。代表性截图保存在同目录；完整临时截图位于 `/tmp/reposignal-style-evidence`。

- 1440px：历史日期 09-18 的整行浅底、竖条与字重正确，“最新”仍属于 09-19；日期键盘焦点可见。归档折叠、展开可通过 Enter 操作，收起后长字段仍限制在 660px（15px × 44em）。hover 样式保持相同边框占位，不改变尺寸。
- 390px：原生日期选择器可导航，原生详情可通过键盘展开。
- 320px：Tab 可将最后一项导航自动滚动到可见范围，焦点轮廓完整。五类报告在外部字体被 CSP 阻止时均无整页横向溢出，`document.fonts.size` 为 0，使用已有系统回退链。
- 搜索：输入 TypeScript 后可通过方向键选择结果；无匹配查询显示空状态；关闭按钮的 Enter 操作将焦点恢复到搜索入口。
- 临时边界样例使用真实 ProjectEntry 渲染器：长仓库名、长链接、长段落、缺失技术栈及风险字段均可换行，推荐字段仍为 500 字重。0 分显示空刻度；86 分八段满、第九段 60%；100 分全部填满；缺失评分显示“周选”。临时测试文件和本地验证页面已删除，未增加产品路由或修改报告内容。

## CSP、无脚本与辅助功能

构建预览在 127.0.0.1:4322 实际发送 `dist/_headers` 中的生产 CSP。刻度使用外部 CSS 与 SVG 属性正常显示，无行内样式、额外脚本或策略放宽。

临时预览的 `verify=no-js` 模式额外施加 `sandbox allow-same-origin`，真实禁用脚本执行及启用 noscript。五类报告正文、归档备用链接均保留，原生 details 可操作；五类页面在 320px 无整页溢出。结果见 `evidence/no-js.json`。字体失败模式仅在本地预览收紧 font-src/style-src，结果见 `evidence/font-fallback.json`。

200% CSS zoom 的临时日报样例中，搜索输入、方向键、关闭并恢复焦点、原生详情 Enter 操作通过，页面宽度未溢出。截图：`evidence/css-zoom-200.png`。这不能替代浏览器原生缩放验收。

**仍待人工验收（任务 7.4 保持未勾选）：** 当前浏览器工具未暴露原生页面缩放和系统减少动态效果偏好的控制能力。原生 200% 缩放、系统减少动态效果切换未完成实际验证。既有 reduced-motion CSS 已审阅：禁用平滑滚动，并将 transition/animation 缩短至 .01ms；本次没有新增动画。工具发送 Escape 未触发原生 dialog 关闭，关闭按钮键盘路径正常，未据此声称 Escape 行为通过。

## 审查与修正

独立代码审查发现月报的变更说明、章节导语和研究说明遗漏行长约束，以及窄屏导航外侧焦点可能被裁切。已修正并在浏览器复现位置重新验证：月报对应段落最大宽度 660px；导航使用内侧焦点轮廓，320px 截图完整。复审未发现新的实质问题。

本次代码范围为共享 CSS、ProjectEntry 及必要的组件渲染/布局测试与 Vitest Astro 配置。自动提交中混入的既有报告和脚本改动不属于本次样式工作；本轮未修改它们。没有执行发布。
