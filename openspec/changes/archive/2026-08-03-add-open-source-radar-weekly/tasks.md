## 1. 雷达周刊内容模型与首期内容

- [x] 1.1 为雷达周刊解析器补充失败优先测试，覆盖合法文章解析、ISO 周文件名、按周倒序、缺少一级标题或封面、残留 `SITE_BASE_URL` 或 UTM 参数，以及内部草稿不被发现。
- [x] 1.2 新增 `src/lib/radar.ts`，实现独立的 `RadarReport` 类型、Markdown 发现与解析、正文预处理和内容校验，使解析器测试通过。
- [x] 1.3 将已确认的首期文章固化为 `data/github-project-digest/radar/2026-W31.md`，使用站点根路径封面、站内相对周报链接，并确认封面资源和 10 个项目链接完整。

## 2. 顶部周期导航

- [x] 2.1 扩展周期链接测试，覆盖“开源雷达周刊”的最新一期链接、无内容时的索引回退、四个标签的顺序和当前项状态。
- [x] 2.2 扩展 `ReportPeriod` 与周期链接解析逻辑，并更新现有月报、周报、日报页面及索引页，使它们都能提供四类周期链接。
- [x] 2.3 更新 `PeriodSwitcher.astro`，新增完整标签“开源雷达周刊”、雷达激活态及可访问性属性，并使相关组件测试通过。

## 3. 日期归档与静态路由

- [x] 3.1 增加日期栏与路由契约测试，覆盖雷达周列表、当前期与最新期标记、移动端选择器、无 JavaScript 归档链接和静态页面生成。
- [x] 3.2 泛化 `DateRail.astro` 的输入结构并实现 `radar` 模式，生成 `/radar/YYYY-Www/` 链接，同时保持现有周报和日报行为不变。
- [x] 3.3 新增连续文章布局组件 `RadarReportView.astro`，展示封面、标题、周次与 Markdown 正文，并接入共享导航和日期栏。
- [x] 3.4 新增 `/radar/[week]/` 静态详情页和 `/radar/` 最新一期入口，设置正确的标题、描述、canonical 和不存在内容时的安全回退。
- [x] 3.5 增加构建结果断言，确认 `2026-W31` 路由生成，且雷达文章不会作为重复项目加入现有项目搜索索引。

## 4. 响应式样式与视觉一致性

- [x] 4.1 增加布局样式契约测试，覆盖移动端两行页头、完整标签、触控高度、封面比例、正文阅读宽度以及现有配色约束。
- [x] 4.2 实现雷达周刊页面样式和移动端两行页头，复用现有色板与组件语言，保证桌面端、移动端和无 JavaScript 状态可读。
- [x] 4.3 在桌面和移动视口检查首页、现有报告页与雷达周刊页，修正溢出、对齐、封面裁切或归档交互问题。

## 5. 完整验证

- [x] 5.1 运行雷达解析、周期导航、日期栏和布局契约的定向测试，并修复所有回归。
- [x] 5.2 运行完整测试、Astro 类型检查和生产构建，确认现有月报、周报、日报及搜索功能保持通过。
- [x] 5.3 检查构建后的雷达周刊 HTML，确认 canonical、封面、10 个 GitHub 项目链接、站内周报链接和无脚本归档存在，且没有占位域名或 UTM 参数。
