## 1. 生成脚本：闭环门槛与双评分

- [x] 1.1 为 12 个能力面定义输入输出（`CAPABILITY_IO`）、交接表（`CAPABILITY_HANDOFFS`）与部署形态（`DEPLOY_FACES`）
- [x] 1.2 实现 `interface_evidence()` 与 `combo_flow()`：组件 ≥3、接口依据、能力面不重复、弱连通无环、起终点齐备
- [x] 1.3 把单一评分拆成 `tech_score()`（技术组合成熟度）与 `plan_evidence()`（需求证据强度），两者互不合并
- [x] 1.4 生成器在方案表中输出输入/输出/上下游与接入方式

## 2. 生成脚本：证据等级、判断与可证伪 MVP

- [x] 2.1 读取 `feedback.jsonl`（`source=user`）作为“已确认事实”来源，实现四级证据阶梯与分项计分
- [x] 2.2 实现四档判断 `plan_judgment()`，并让“暂不建议”不进入方案列表
- [x] 2.3 实现 `build_experiment()`：测试场景/测试数据/周期/成功指标/失败指标/停止条件
- [x] 2.4 生成“最大不确定性”与“下一步动作”，按判断给出不同动作
- [x] 2.5 模板市场文案改写为带证据等级的表述，新增 `FORBIDDEN_CLAIMS` 与 `sanitize_claims()`

## 3. 生成脚本：冷却与结构重排

- [x] 3.1 用 `load_family_history()` + `cooldown_decision()` 取代“连续两天”新鲜度规则（7–14 天冷却 + 提前重现条件）
- [x] 3.2 删除“单点项目机会”“行动建议”与 `TAG_ANGLE`，新增报告级“今日结论”
- [x] 3.3 重排方案块顺序为 方案判断 → 客户问题 → 组件数据流 → 双评分 → MVP 实验 → 最大不确定性 → 下一步动作
- [x] 3.4 允许 0 个方案：今日结论列出冷却跳过、暂不建议、闭环门槛未过的候选与原因

## 4. 测试

- [x] 4.1 更新渲染、选择器、命名与锚点测试到新签名与新字段
- [x] 4.2 新增闭环门槛、双评分、判断阶梯、证据等级、MVP、冷却与报告结构测试
- [x] 4.3 更新 KUN-TASK 契约测试（0–3 个、双评分、四档判断、冷却、证据等级、禁用措辞）

## 5. 任务规则与定时任务入口

- [x] 5.1 重写 `KUN-TASK.md`：目标、输入、执行步骤、产出、验证标准与边界对齐新口径
- [x] 5.2 更新 Kun 定时任务入口提示的命名步骤、核对清单与汇报格式

## 6. 站点呈现

- [x] 6.1 解析并展示方案判断与双评分面板，摘要字段改为方案判断/目标客户/需求证据
- [x] 6.2 保持历史报告（单一评分、单点机会、行动建议）渲染兼容
- [x] 6.3 补充响应式样式与布局契约测试

## 7. 验证

- [x] 7.1 运行 Python 全量测试、vitest、类型检查与构建
- [x] 7.2 用历史日期在临时数据根生成样本报告，核对新结构并验证网页解析器可解析生成结果
- [x] 7.3 校验 OpenSpec 变更并归档
